To build source distribution:
    python setup.py sdist

To build rpm distribution:
    python setup.py bdist_rpm

To build docker distribution:
    python setup.py bdist_rpm
    docker build -t netcontrold .
    docker run --name ncd --pid=host -v /etc/openvswitch:/etc/openvswitch -v /var/lib/vhost_sockets:/var/lib/vhost_sockets -v /var/run/openvswitch:/var/run/openvswitch -dit netcontrold:latest
    docker attach ncd [ctrl-p + ctrl-q to detach]
